<Page
    x:Class="DDSStudyOS.App.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20" Spacing="4">
            <TextBlock Text="DDS StudyOS"
                       Style="{StaticResource TitleTextBlockStyle}"
                       Foreground="{ThemeResource SystemControlForegroundAccentBrush}" />
            <TextBlock Text="Seu sistema operacional de conhecimento."
                       Style="{StaticResource BodyTextBlockStyle}"
                       Opacity="0.7" />
        </StackPanel>

        <Grid Grid.Row="1" ColumnSpacing="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Spacing="16">
                <Border Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
                        CornerRadius="8"
                        Padding="16">
                    <StackPanel Spacing="8">
                        <TextBlock Text="CONTINUAR ESTUDANDO"
                                   FontSize="12"
                                   Opacity="0.65"
                                   FontWeight="Bold" />
                        <TextBlock x:Name="LastCourseTitle"
                                   Text="Nenhum curso recente"
                                   FontSize="28"
                                   FontWeight="SemiBold"
                                   TextWrapping="Wrap"
                                   TextTrimming="CharacterEllipsis" />
                        <TextBlock x:Name="LastCoursePlatform"
                                   Text="Comece agora!"
                                   Opacity="0.8" />
                        <Button x:Name="ContinueBtn"
                                Content="Retomar"
                                HorizontalAlignment="Left"
                                Style="{StaticResource AccentButtonStyle}"
                                Click="Continue_Click"
                                IsEnabled="False" />
                    </StackPanel>
                </Border>

                <TextBlock Text="Acesso Rápido" Style="{StaticResource SubtitleTextBlockStyle}" />

                <StackPanel Orientation="Horizontal" Spacing="10">
                    <Button Content="Novo Curso" Tag="new_course" Click="QuickActionButton_Click" />
                    <Button Content="Ver Agenda" Tag="agenda" Click="QuickActionButton_Click" />
                    <Button Content="Navegador" Tag="browser" Click="QuickActionButton_Click" />
                    <Button Content="Favoritos" Tag="browser_favorites" Click="QuickActionButton_Click" />
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1" Spacing="16">
                <Border Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
                        CornerRadius="8"
                        Padding="14">
                    <StackPanel HorizontalAlignment="Center" Spacing="4">
                        <TextBlock Text="Sequência" FontWeight="SemiBold" Opacity="0.8" />
                        <TextBlock x:Name="StreakText"
                                   Text="0"
                                   FontSize="32"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center" />
                        <TextBlock Text="dias seguidos" Opacity="0.7" />
                    </StackPanel>
                </Border>

                <Border Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
                        CornerRadius="8"
                        Padding="14">
                    <StackPanel Spacing="6">
                        <TextBlock Text="Meta semanal"
                                   FontWeight="SemiBold"
                                   Opacity="0.85" />
                        <TextBlock x:Name="WeeklyGoalDaysText"
                                   Text="0/5 dias"
                                   FontWeight="SemiBold" />
                        <TextBlock x:Name="WeeklyGoalMinutesText"
                                   Text="0/450 min"
                                   Opacity="0.8" />
                        <ProgressBar x:Name="WeeklyGoalProgressBar"
                                     Minimum="0"
                                     Maximum="100"
                                     Value="0"
                                     Height="6" />
                        <TextBlock x:Name="WeeklyGoalSummaryText"
                                   Text="Acompanhe sua consistência semanal."
                                   TextWrapping="WrapWholeWords"
                                   Opacity="0.75"
                                   FontSize="12" />
                    </StackPanel>
                </Border>

                <StackPanel>
                    <TextBlock Text="Próximos Lembretes" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,6" />
                    <ListView x:Name="RemindersList" Height="280" SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Spacing="2" Padding="0,4">
                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding DueAt}" FontSize="12" Opacity="0.7" />
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
